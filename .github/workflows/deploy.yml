name: 🚀 Auto-Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: 🟢 Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: 📦 Install dependencies
        run: npm ci --legacy-peer-deps
      
      - name: 🔨 Build project
        run: npm run build
        env:
          NODE_OPTIONS: '--max-old-space-size=4096'
      
      - name: ✅ Verify build output
        run: |
          echo "✅ Build completed successfully!"
          echo "📊 Build stats:"
          ls -lh dist/
          echo ""
          echo "📄 Files in dist:"
          find dist -type f -name "*.html" -o -name "*.js" -o -name "*.css" | head -20
          echo ""
          if [ -f "dist/iptv-player.html" ]; then
            echo "✅ IPTV Player found in dist"
          else
            echo "⚠️ WARNING: IPTV Player not found!"
          fi
      
      - name: 🚀 Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages
          clean: true
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: 🎉 Deployment complete
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Deployment successful!"
          echo "🌐 Homepage: https://dlnraja.github.io/xpengmedia/"
          echo "🎬 IPTV: https://dlnraja.github.io/xpengmedia/iptv-player.html"
          echo "⏰ Deployed at: $(date)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
